hello: "C'est le mode dev"

spring:
  profiles: default

server:
  port: 8080

mysql:
  url: "jdbc:mysql://localhost:3306/BLOG_JAVA?serverTimezone=GMT"
  username: blog_root
  password: egh5ohCuey0o

---
hello: "C'est la prod"

spring:
  profiles: prod

server:
  port: 8080

mysql:
  url: ${APP_URL}
  username: ${APP_USERNAME}
  password: ${APP_PASSWORD}


